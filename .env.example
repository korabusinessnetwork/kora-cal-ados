# Copie para .env.local e preencha. NUNCA commitar .env* (ver .gitignore).
# Regra do projeto: nada de chave hardcodada no código, sempre import.meta.env.VITE_*

# --- Cliente (navegador), só chave pública, nunca service_role ---
VITE_SUPABASE_URL=https://SEU-PROJETO.supabase.co
VITE_SUPABASE_ANON_KEY=

# --- Servidor e testes, SEM prefixo VITE_ ---
# O prefixo VITE_ expõe a variável no bundle do navegador. A service_role dá acesso
# total ao banco ignorando RLS, então ela NUNCA pode carregar esse prefixo.
# Ver docs/11_SEGURANCA/multi-tenancy-rls.md.
#
# As três abaixo também alimentam o teste de isolamento (supabase/tests/). Sem elas,
# o teste PULA em vez de passar em falso.
#
# SUPABASE_URL e SUPABASE_SERVICE_ROLE_KEY são também o que a API de variante lê
# (api/), tanto no servidor local quanto no ambiente de deploy: depois de validar a
# chave de API do tenant, ela consulta com service_role, que bypassa a RLS, e por isso
# o tenant_id sai SEMPRE da chave, nunca do pedido (ADR-006).
SUPABASE_URL=https://SEU-PROJETO.supabase.co
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Cifra da chave do fornecedor de modelo de linguagem de cada marca (D13), 32 bytes em base64.
# Só servidor, SEM prefixo VITE_: com ela e um dump da tabela, a chave de toda marca sai em claro.
# Trocar esta chave invalida as chaves gravadas (cada owner cola a dele de novo).
# Gere com: node -e "console.log(require('node:crypto').randomBytes(32).toString('base64'))"
CHAVE_DE_CIFRA_DOS_FORNECEDORES=
