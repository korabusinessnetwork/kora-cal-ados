{
  "name": "kora-calcados",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "Motor de customizacao de calcados - editor de zonas + API de geracao de variante",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "prototipo:recolor": "node scripts/prototipo-recolor-demo.mjs"
  },
  "dependencies": {
    "jsdom": "^30.0.1"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
